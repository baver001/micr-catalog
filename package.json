{
  "name": "micr-fun",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:tail": "wrangler tail",
    "deploy": "npm run build && netlify deploy --prod",
    "deploy:all": "wrangler deploy && npm run deploy"
  },
  "devDependencies": {
    "netlify-cli": "^23.13.1",
    "vite": "^6.0.3",
    "vite-plugin-pwa": "^0.21.1",
    "wrangler": "^4.57.0"
  },
  "dependencies": {
    "lucide": "^0.562.0"
  }
}
